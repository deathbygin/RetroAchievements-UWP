<Page
    x:Class="RetroAchievements_UWP.GameInfoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RetroAchievements_UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    XYFocusKeyboardNavigation="Enabled"
    KeyDown="Page_KeyDown">

    <Grid x:Name="RootGrid">
        <Viewbox Stretch="Uniform" HorizontalAlignment="Stretch" VerticalAlignment="Top">
            <Grid Width="1400" Height="800" Padding="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Profile Information Section with Image (Sticky Header) -->
                <Grid Grid.Row="0" Margin="10,0,20,0">
                    <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Orientation="Horizontal" Spacing="20">
                        <Button x:Name="BackButton" Content="Back" Click="BackButton_Click" Height="84" VerticalAlignment="Top" CornerRadius="4" FontWeight="Semibold"/>
                        <!-- User Profile Image -->
                        <Image x:Name="UserProfileImage" Width="84" Height="84" Stretch="UniformToFill"/>

                        <!-- Profile Text Information -->
                        <StackPanel Orientation="Vertical" Spacing="4">
                            <TextBlock Text="User Profile" FontSize="20" FontWeight="Semibold"/>
                            <TextBlock x:Name="UserTextBlock" FontSize="11"/>
                            <TextBlock x:Name="MemberSinceTextBlock" FontSize="11"/>
                            <TextBlock x:Name="MottoTextBlock" FontSize="11"/>
                            <TextBlock x:Name="TotalPointsTextBlock" FontSize="11"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- App Title Text -->
                    <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock x:Name="AppTitleTextBlock" Text="RetroAchievements" 
                                   FontSize="28" 
                                   FontWeight="Bold" 
                                   HorizontalAlignment="Right"/>
                        <TextBlock Text="v1.0.0" 
                                   FontSize="14" 
                                   Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" 
                                   HorizontalAlignment="Right"/>
                    </StackPanel>
                </Grid>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,20,0,0">
                    <StackPanel Orientation="Vertical">
                        <Grid>
                            <StackPanel Orientation="Horizontal" Spacing="20" HorizontalAlignment="Left">
                                <Image x:Name="GameIconImage" Width="120" Height="120" Stretch="UniformToFill" VerticalAlignment="Top"/>
                                <StackPanel Orientation="Vertical">
                                    <TextBlock x:Name="GameTitleTextBlock" FontSize="36" FontWeight="Bold"/>
                                    <TextBlock x:Name="ConsoleNameTextBlock" FontSize="24" Margin="0,5,0,0" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                                    <TextBlock x:Name="GameIdTextBlock" FontSize="20" Margin="0,10,0,0"/>
                                </StackPanel>
                            </StackPanel>
                            <Button x:Name="LeaderboardButton" Content="Leaderboard" HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="20" Padding="30,15" Margin="0,0,50,0" Click="LeaderboardButton_Click" CornerRadius="4" FontWeight="Semibold"/>
                        </Grid>
                        <StackPanel Margin="0,20,0,0" Width="400" HorizontalAlignment="Left">
                            <TextBlock x:Name="AchievementProgressTextBlock" FontSize="18" Margin="0,0,0,5"/>
                            <ProgressBar x:Name="AchievementProgressBar" Height="10" CornerRadius="4"/>
                        </StackPanel>
                        
                        <TextBlock Text="Achievements" FontSize="28" FontWeight="Semibold" Margin="0,30,0,10"/>
                        
                        <GridView x:Name="AchievementsGridView" 
                                  VerticalAlignment="Stretch"
                                  ScrollViewer.HorizontalScrollMode="Disabled"
                                  ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                                  ScrollViewer.VerticalScrollMode="Enabled"
                                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                                  ContainerContentChanging="OnContainerContentChanging">
                    <GridView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsWrapGrid Orientation="Horizontal" MaximumRowsOrColumns="11"/>
                        </ItemsPanelTemplate>
                    </GridView.ItemsPanel>
                    <GridView.ItemTemplate>
                        <DataTemplate>
                            <Image Source="{Binding ImageUrl}" Width="100" Height="100" Margin="10" Stretch="UniformToFill">
                                <ToolTipService.ToolTip>
                                    <ToolTip Padding="6">
                                        <StackPanel Spacing="3">
                                             <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="18"/>
                                             <TextBlock Text="{Binding Description}" FontStyle="Italic" FontSize="14" TextWrapping="Wrap" MaxWidth="300"/>
                                             <TextBlock Text="{Binding Points}" FontSize="14"/>
                                             <TextBlock Text="{Binding DateEarnedText}" FontSize="12" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                                        </StackPanel>
                                    </ToolTip>
                                </ToolTipService.ToolTip>
                            </Image>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Viewbox>
    </Grid>
</Page>
